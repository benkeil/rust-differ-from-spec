[package]
name = "differ-from-spec-test"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
differ-from-spec = { path = "../" }

[dev-dependencies]
#rstest = "0.18.2"